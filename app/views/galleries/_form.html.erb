<div class="container">
  <div class="row">
    <div class="col-sm-5 form-group">
        <%= simple_form_for :gallery, defaults: { input_html: { class: 'form-control' } }  do |f| %>
        <%= f.input :title %>
        <%= f.input :description %>
        <%= f.button :submit, class: "btn btn-primary" %>
        <% end %>
    </div>
  </div>
</div>
